services:
  - type: web
    name: scarabheart-web
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:extension
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
